{
  "name": "ronny-christensen-resume",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html index.tsx App.tsx types.ts metadata.json dist/ && cp -r components data i18n lib dist/"
  },
  "dependencies": {
    "@google/genai": "0.14.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.6.0",
    "typescript": "^5.4.5"
  }
}