{
  "name": "digital-resume",
  "version": "1.0.0",
  "description": "A modern, single-page digital resume",
  "scripts": {
    "build": "tsc && mkdir -p dist && cp index.html metadata.json dist/ && cp -r components data i18n lib dist/"
  },
  "dependencies": {
    "@google/genai": "^1.15.0",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.2",
    "@types/html2pdf.js": "^0.10.0",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7"
  }
}
